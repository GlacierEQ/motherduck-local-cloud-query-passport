[project]
name = "motherduck-local-cloud-query-passport"
version = "0.0.1"
description = "Scaffold: Local-Cloud Query Passport (independent MotherDuck lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
